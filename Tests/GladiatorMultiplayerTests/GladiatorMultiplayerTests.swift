import XCTest
@testable import GladiatorMultiplayer

final class GladiatorMultiplayerTests: XCTestCase {
    func testExample() {
        // This is an example of a functional test case.
        // Use XCTAssert and related functions to verify your tests produce the correct
        // results.
        XCTAssertEqual(GladiatorMultiplayer().text, "Hello, World!")
    }

    static var allTests = [
        ("testExample", testExample),
    ]
}
